version: 2.0
runtime: python3

build:
  commands:
    build:
      - yum update -y
      - yum install -y git
      - pip install -r requirements.txt
 
run:
  command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker lib:app --log-level Debug -b 0.0.0.0:3000
  network:
    port: 3000 
